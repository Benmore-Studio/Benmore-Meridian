# DESIGN-AUDIT — <project> design backlog

> Generated by `design-director` survey on <date>. This is the live worklist.
> The skill checks items off here as it fixes them. Re-run the survey after a
> batch of fixes to refresh scores. Severity: P0 blocking · P1 major · P2 minor
> · P3 polish. Each issue names the `impeccable` command that fixes it.

## Scorecard

| Metric | Value |
|---|---|
| Critique (UX, Nielsen) | ??/40 |
| Audit (technical) | ??/20 |
| Surfaces reviewed | ?? |
| Open issues | P0:?? · P1:?? · P2:?? · P3:?? |
| Opportunities (additions) | ?? |
| Contract | DESIGN.md / brand kit / inline — <which> |

## Surface map

Every route / page / key component reviewed (so coverage is auditable):

- [ ] `/` — <name>
- [ ] `/...` — <name>
- [ ] Component: `<Name>`

## Issue register

Work top-down (P0 → P3). Tick the box when fixed and verified.

| ID | Surface | Category | Term | Symptom | Sev | Fix command | Done |
|----|---------|----------|------|---------|-----|-------------|------|
| 1  | `/dashboard` | Color | semantic token | Raw `#888` used for muted text, ×12 across pages | P1 | `colorize` + contract-extend | [ ] |
| 2  | `/dashboard` | States | empty state | Table shows nothing on first run, no next action | P1 | `onboard` | [ ] |
| 3  | `/dashboard` | A11y | focus state | Icon buttons have no visible focus ring | P0 | `harden` | [ ] |
| 4  | `/settings` | Typography | tabular nums | Plan prices misalign as values change | P2 | `typeset` | [ ] |
| …  | | | | | | | [ ] |

## Opportunity inventory (additions the user did not ask for)

Each must name the user question it answers. No decorative additions.

| ID | Surface | Addition | User question it answers | Build via | Done |
|----|---------|----------|--------------------------|-----------|------|
| A1 | `/dashboard` | Replace card grid with **data table** | "Which records changed and by how much?" | `craft` | [ ] |
| A2 | `/dashboard` | **Usage trend chart** | "Is my usage going up or down?" | `craft` | [ ] |
| A3 | `/records/:id` | **Inspector drawer** | "What are the details without leaving the list?" | `craft` | [ ] |
| A4 | global | **Command menu** (⌘K) | "How do I jump anywhere fast?" | `craft` | [ ] |
| …  | | | | | [ ] |

## Patterns & systemic issues

Recurring problems that indicate a systemic gap, not a one-off:

- e.g. "Hard-coded grays in 15+ components — should be `--color-fg-muted` token."
- e.g. "Touch targets consistently < 44px across mobile."

## Contract delta

Tokens/components added or extended in `DESIGN.md` to resolve drift during fixes:

- `--color-fg-muted: <hex>` (replaces ad-hoc grays)
- `--radius-md`, `--shadow-card` (replaces one-off values)

## Positive findings

What already works and should be preserved / replicated.
